<odoo>
    <data>
        <!-- explicit list view definition -->
        <!--
            <record model="ir.ui.view" id="uninstall.list">
              <field name="name">uninstall list</field>
              <field name="model">uninstall.uninstall</field>
              <field name="arch" type="xml">
                <tree>
                  <field name="name"/>
                  <field name="value"/>
                  <field name="value2"/>
                </tree>
              </field>
            </record>
        -->

        <!-- actions opening views on models -->
        <!--
            <record model="ir.actions.act_window" id="uninstall.action_window">
              <field name="name">uninstall window</field>
              <field name="res_model">uninstall.uninstall</field>
              <field name="view_mode">tree,form</field>
            </record>
        -->

        <!-- server action to the one above -->
        <!--
            <record model="ir.actions.server" id="uninstall.action_server">
              <field name="name">uninstall server</field>
              <field name="model_id" ref="model_uninstall_uninstall"/>
              <field name="state">code</field>
              <field name="code">
                action = {
                  "type": "ir.actions.act_window",
                  "view_mode": "tree,form",
                  "res_model": model._name,
                }
              </field>
            </record>
        -->

        <!-- Top menu item -->
        <!--
            <menuitem name="uninstall" id="uninstall.menu_root"/>
        -->
        <!-- menu categories -->
        <!--
            <menuitem name="Menu 1" id="uninstall.menu_1" parent="uninstall.menu_root"/>
            <menuitem name="Menu 2" id="uninstall.menu_2" parent="uninstall.menu_root"/>
        -->
        <!-- actions -->
        <!--
            <menuitem name="List" id="uninstall.menu_1_list" parent="uninstall.menu_1"
                      action="uninstall.action_window"/>
            <menuitem name="Server to list" id="uninstall" parent="uninstall.menu_2"
                      action="uninstall.action_server"/>
        -->
    </data>
</odoo>